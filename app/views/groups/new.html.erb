
<h1>As we were saying, add a new group...</h1>

<%= form_tag('/groups', :method => "post") do %>
  <%= label_tag(:name, 'Name:') %>
  <%= text_field_tag(:name) %>
  <%= submit_tag('Create') %>
<% end %>
